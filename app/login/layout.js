export const metadata = {
  title: "Iniciar Sesión",
  description: "Inicia sesión en tu cuenta de Alianza Puembo para acceder al panel de administración.",
  robots: { 
    index: false, 
    follow: false 
  },
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
