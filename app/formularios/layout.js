export const metadata = {
  title: "Formularios",
  description: "Formularios de registro para eventos y actividades de Alianza Puembo.",
};

export default function FormulariosLayout({ children }) {
  return <>{children}</>;
}
