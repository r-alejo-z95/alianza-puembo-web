export const metadata = {
  title: "Vista Previa de Formulario",
  description: "Previsualización del formulario en construcción.",
  robots: { 
    index: false, 
    follow: false 
  },
};

export default function PreviewLayout({ children }) {
  return <>{children}</>;
}
