export const metadata = {
  title: "Constructor de Formulario",
  description: "Crea y edita formularios de registro dinámicos.",
  robots: { 
    index: false, 
    follow: false 
  },
};

export default function BuilderLayout({ children }) {
  return <>{children}</>;
}
